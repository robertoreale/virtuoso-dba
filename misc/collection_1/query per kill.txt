SELECT s.inst_id,
       s.sid,
       s.serial#,
       p.spid,
       s.username,
       s.program, 
       s.event
FROM   gv$session s
       JOIN gv$process p ON p.addr = s.paddr AND p.inst_id = s.inst_id
WHERE  s.type != 'BACKGROUND' and s.username='STCR_APPPRD' order by 2;


